traits:
  asdf-ruby: true
  Bash: true

scripts:
  mail-setup-ruby:
    exclusive_executor_resource: ruby_{{RUBY}}
    timeout: 20 Minutes
    body: |
      #!/usr/bin/env bash
      set -euxo
      cd $LEIHS_MAIL_DIR
      asdf install ruby
      bundle install
