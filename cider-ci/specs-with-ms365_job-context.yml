generate_tasks:
  include_match: spec/ms365/.*_spec.rb
task_defaults:
  include: cider-ci/task-components/fake-ms365-server.yml
  scripts:
    test:
      body: |
        #!/usr/bin/env bash
        set -euo pipefail
        cd $LEIHS_MAIL_DIR

        unset PGPORT; unset PGUSER
        PGPORT=${PG15PORT} PGUSER=${PG15USER} \
          ./bin/rspec $CIDER_CI_TASK_FILE
