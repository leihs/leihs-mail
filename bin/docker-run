#!/bin/bash
set -exu

WD="$(pwd)"

IMAGE="$(IMAGE_NAME=leihs-mail-builder STAGE=builder bin/docker-build | tail -1)"
test "$IMAGE"

docker run --rm -it \
  -v "${WD}/:/leihs/mail/" \
  "$IMAGE" \
  $*